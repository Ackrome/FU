-- 1. Выбор всех строк из таблицы Users
SELECT * FROM Users;

-- 2. Выбор всех строк из таблицы Artists
SELECT * FROM Artists;

-- 3. Выбор всех строк из таблицы Albums
SELECT * FROM Albums;

-- 4. Выбор всех строк из таблицы Songs
SELECT * FROM Songs;

-- 5. Выбор всех строк из таблицы Playlists
SELECT * FROM Playlists;